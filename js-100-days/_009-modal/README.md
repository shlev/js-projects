css: z-index poriority

without z-index elements stacking in order first->lowest
* positioned elements get z-index (static excluded)
* transorm set default z-index to 0
* child is limited to its parent z-index